# Beepd API (Cloudflare Workers) — local dev vars
#
# Wrangler reads local secrets from a file named `.dev.vars` in this folder.
# If you want to use these locally:
#   cp .env.example .dev.vars
#
# NOTE: Do not commit real secrets.

# Environment name (used for logging/feature flags)
APP_ENV=dev

# Comma-separated list of allowed origins for CORS during local dev.
# Add your LAN IP origins if testing web/mobile on another device.
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4321

# Auth/session secrets (placeholders; will be required once auth is implemented)
# Generate strong random values before staging/prod.
AUTH_JWT_SIGNING_SECRET=change-me
AUTH_REFRESH_TOKEN_ENCRYPTION_KEY=change-me

# OTP providers (placeholders; will be wired in Phase 4)
OTP_EMAIL_PROVIDER=stub
OTP_SMS_PROVIDER=stub

# Example provider secrets (leave blank unless using that provider)
# RESEND_API_KEY=
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_FROM_NUMBER=
