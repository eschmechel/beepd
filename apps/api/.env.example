# Beepd API (Cloudflare Workers) — local dev vars
#
# Wrangler reads local secrets from a file named `.dev.vars` in this folder.
# If you want to use these locally:
#   cp .env.example .dev.vars
#
# NOTE: Do not commit real secrets.

# Drizzle Kit (optional)
#
# Only needed if you use drizzle-kit's D1 HTTP driver (push/pull) against a remote
# database. Generating migration SQL from schema changes does not require these.
#
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_DATABASE_ID=
# CLOUDFLARE_D1_TOKEN=

# Environment name (used for logging/feature flags)
APP_ENV=dev

# Logging level
LOG_LEVEL=info

# Comma-separated list of allowed origins for CORS during local dev.
# Add your LAN IP origins if testing web/mobile on another device.
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4321

# =============================================================================
# SESSION / TOKEN CONFIG
# =============================================================================

# Iron-session secret: base64-encoded 32+ byte key for signing/encrypting tokens.
# Generate: `openssl rand -base64 32`
SESSION_SECRET=change-me-in-dev-vars

# Token lifetimes (in seconds)
AUTH_ACCESS_TOKEN_TTL_SECONDS=900      # 15 minutes
AUTH_REFRESH_TOKEN_TTL_SECONDS=2592000  # 30 days

# =============================================================================
# OTP (Email via Resend)
# =============================================================================
#
# OTP configuration is HARDCODED in apps/api/src/services/otp.ts:
#   - Code length: 6 characters
#   - Code lifetime: 5 minutes
#   - Resend cooldown: 60 seconds
#   - Max attempts: 5 per challenge
#
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=

# =============================================================================
# OAUTH PROVIDERS (v0.1)
# =============================================================================

# Google OAuth (https://console.cloud.google.com/apis/credentials)
OAUTH_GOOGLE_CLIENT_ID=
OAUTH_GOOGLE_CLIENT_SECRET=

# GitHub OAuth (https://github.com/settings/developers)
OAUTH_GITHUB_CLIENT_ID=
OAUTH_GITHUB_CLIENT_SECRET=

# =============================================================================
# DEFERRED TO v1.2
# =============================================================================

# Microsoft OAuth (requires Azure - crashes frequently)
# OAUTH_MICROSOFT_CLIENT_ID=
# OAUTH_MICROSOFT_CLIENT_SECRET=

# Apple Sign In (requires $120/yr Apple Developer account)
# OAUTH_APPLE_CLIENT_ID=
# OAUTH_APPLE_CLIENT_SECRET=
# OAUTH_APPLE_TEAM_ID=
# OAUTH_APPLE_KEY_ID=
# OAUTH_APPLE_PRIVATE_KEY=

# =============================================================================
# STUBBED PROVIDERS (Phone SMS - v1.1)
# =============================================================================

TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
