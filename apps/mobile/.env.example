# Beepd Mobile (Expo) — env vars
#
# Expo only exposes variables prefixed with EXPO_PUBLIC_ to the app bundle.
# If you run on a physical Android device, localhost will NOT point at your laptop.
# Use your LAN IP instead (e.g. http://192.168.1.70:8081).

EXPO_PUBLIC_API_BASE_URL=http://localhost:8081
