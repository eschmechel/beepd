# Beepd Web (Vite) — env vars
#
# Vite only exposes variables prefixed with VITE_ to the browser.

# API base URL
VITE_API_BASE_URL=http://localhost:8787

# Optional: map provider key (when map UI is implemented)
# VITE_MAPTILER_KEY=
