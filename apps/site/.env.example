# Beepd Site (Astro) — env vars
#
# Astro exposes variables prefixed with PUBLIC_ to client-side code.

# These are primarily for links/CTAs and any client-side fetches the site may do.

PUBLIC_WEB_APP_URL=http://localhost:5173
PUBLIC_API_BASE_URL=http://localhost:8787
